[pytest]
# Only collect tests from the tests/ directory to avoid duplicate module names
testpaths = tests
# Explicitly ignore legacy top-level test files that have been moved
collect_ignore = test_pet.py test_engine_smoke.py test_time_scaling.py
